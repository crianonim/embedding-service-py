# Docker configuration (used by docker-compose)
# DATABASE_URL defaults to postgresql://postgres:postgres@db:5432/embeddings
#
# For local development, create config.local.env with:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/embeddings
